# Algo-Assignment-3
To run the python script enter the following command
python interleaving.py -il input/isInterleaved.txt
or
python interleaving.py -il input/notInterleaved.txt
or
python interleaviny.py -il <Path to txt file>

The format if the input files are the following format

The Python script is delimeted by a space
x-String y-string s-string

The Python script will print whether or not the S is an intelreaving of X and Y But
the results of the trace runs will be put into the results folder.

The makeInterleavedInput.py will create interleavedInput solely for the purpose of timing based runs.
